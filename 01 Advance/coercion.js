//JS wanna be smart but dont use this feature

console.log('5' - 6);// in this case it will substract
console.log('4' + 5);  //but in this case JS cannot be so smart ;It only concatinate this
// lets test what is it

const giveType = typeof '5'

console.log(giveType);

const adder = true + 5 // true in jS mean its a literal 1 number
console.log(adder);
const loginDetailed = []
// login for getting rom DB

const loginID = logDetails[0]
if(loginID !== undefigned){
  console.log("allow user to login");
}
elase{
  console.log('Auth  Failed!');
}
